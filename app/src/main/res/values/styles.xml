
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:actionOverflowButtonStyle">@style/MyActionButtonOverflow</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/whitee</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>

        <item name="android:listPreferredItemPaddingLeft">10dp</item>
        <item name="android:listPreferredItemPaddingRight">20dp</item>
    </style>

    <style name="AppTheme.TransparentTheme">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="MyActionButtonOverflow" parent="android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/menu_bar</item>
    </style>

    <style name="CustomTheme" parent="Theme.AppCompat.Light">
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
        <item name="colorPrimary">@color/whitee</item>

    </style>

    <style name="Theme.MyApp" parent="Theme.MaterialComponents.Light">
        <item name="boxStrokeColor">@color/powder_blue</item>

        <!-- ... -->
    </style>
    <style name="textbox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeColor">@color/powder_blue</item>
        <item name="boxStrokeWidth">2dp</item>
        <item name="android:radius">8dp</item>
        <item name="android:color">@color/powder_blue</item>
    </style>








    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
